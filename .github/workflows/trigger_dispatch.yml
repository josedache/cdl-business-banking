name: Trigger Repository Dispatch

on:
  workflow_dispatch:
  push:
    branches:
      - "qa"

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v3
        with:
          event-type: trigger-qa-workflow
          token: ${{ secrets.PAT }}
